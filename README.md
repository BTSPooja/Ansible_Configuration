# Ansible_Configuration(how to configure target server from ansible server)

Launch 2 EC2 instances(Ansible,target)
connect to Ansible server
$ sudo -i 
$ sudo apt update 
$ sudo apt-get install ansible
